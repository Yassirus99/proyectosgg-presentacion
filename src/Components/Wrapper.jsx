import { styled } from "@mui/material";

const Wrapper = styled("div")({
  display: "block",
  width: "100%",
  maxWidth: 1280,
  minHeight: "50px",
  margin: "0 auto",
  padding: "0rem 0rem"
});

export default Wrapper;
